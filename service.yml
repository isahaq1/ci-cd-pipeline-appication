apiVersion: v1
kind: Service
metadata:
  name: nodeapp-testN-service
spec:
  selector:
    app: nodeapp 
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 3000
    nodePort: 31108
