apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeapp-kubernets-isk
  labels:
    app: nodeapp-kubernets-isk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodeapp-kubernets-isk
  template:
    metadata:
      labels:
        app: nodeapp-kubernets-isk 
    spec:
      containers:
      - name: nodeserver
        image: isahaq/cicdtestbttt:202
        ports:
        - containerPort: 3000
        
        
---

apiVersion: v1
kind: Service
metadata:
  name: nodeapp-kubernetsisk-service
spec:
  selector:
    app: nodeapp-kubernets-isk 
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 3000
    nodePort: 31110
